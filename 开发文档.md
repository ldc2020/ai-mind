# 开发文档

## 2026-06-12

1. **优化备注悬浮窗高度与属性面板兼容性**
   - **悬浮窗高度翻倍**: 将备注悬浮层（`.rich-tooltip`）的 `max-height` 限制从 `320px` 调整为 `640px`。同时在 `app.js` 的 `positionRichNoteTooltip` 方法中加入了动态最大高度计算 `Math.min(640, window.innerHeight - 2 * margin)`，以防止在小屏幕设备上悬浮窗高度超出浏览器可视区域导致被截断，进一步完善了阅读体验。
   - **移除重复的工具函数**: 在 `app.js` 中发现了两个同名的 `stripHtml` 函数定义，会导致作用域冲突及不稳定的文本提取行为（特别是在尝试获取纯文本为空时导致属性面板无法正常打开）。现在已将其合并并优化了 `trim()` 的安全调用方式，彻底修复了部分节点因备注提取错误而导致属性面板渲染崩溃、点击节点无响应的问题。

2. **修复仅包含图片等非文本内容的备注无法保存和显示的问题**
   - **问题**: 用户在节点备注中如果只插入了图片（或表格、视频等非纯文本内容）而没有输入任何文字，点击保存后备注会被静默丢弃。这导致节点上不会显示备注图标，右侧属性面板中的“备注”按钮也无法正确高亮显示，用户误以为“备注看不到”。
   - **解决**: 
     - 移除了保存和状态检测时对 `wangEditorInstance.getText().trim()`（纯文本内容）的强依赖。
     - 改为使用现有的 `hasVisibleNoteContent` 函数来判断备注是否为空。该函数不仅检测文本，还能正确识别 `<img>`, `<table>`, `<hr>`, `<iframe>`, `<video>` 等多媒体与结构化标签。
     - 修复后，即使备注中只有一张图片，也能被正确保存、渲染备注图标，并在悬浮时完美呈现自适应宽度的富文本提示框。
     - **追加修复**: 修复了包含图片的备注节点再次点击时右侧属性面板消失（渲染崩溃）的问题。原因为重构检测逻辑时意外移除了用于属性面板按钮文本预览的 `plainNote` 变量导致了 `ReferenceError`。现已恢复该变量，并在纯图片备注时在面板按钮上优雅降级显示为 `[图片/富文本]`。

2. **修复摘要节点备注、标签、超链接等属性更新后需要刷新才能显示的Bug**
   - **问题**: 用户在为摘要节点（或普通节点）添加备注、标签、超链接和图标时，页面不会立即渲染出对应的图标，需要刷新页面后才能看到。
   - **解决**:
     - 弃用了直接调用 `node.setData({ ... })` 后粗暴调用全局 `mindMap.render()` 的方式。
     - 改为调用 `simple-mind-map` 核心库自带的命令流，例如 `mindMap.execCommand('SET_NODE_NOTE', node, finalNote)`、`SET_NODE_TAG`、`SET_NODE_HYPERLINK` 和 `SET_NODE_ICON`。
     - 底层命令不仅会自动处理数据挂载与历史记录入栈，更重要的是会调用 `node.reRender()` 触发节点尺寸（`size`）的重新计算。当节点发现自身尺寸由于新增图标而发生改变时，能正确地触发增量重绘，从而让图标瞬间出现，彻底解决了需刷新才能生效的体验问题。

2. **修复多个摘要节点并存时鼠标悬浮备注内容错乱与选中失效的问题**
   - **问题**: 在同一个父级分支（如 `block层`）下，如果用户框选了不同的子节点并生成了多个摘要节点（例如“哪个在前？”和“流程”），在鼠标悬浮时，所有摘要节点的备注内容都错误地显示为第一个摘要节点的内容。
   - **解决**: 
     1. 深入分析底层渲染逻辑后发现，同一个节点上的多个摘要节点会共享同一个 `generalization_{uid}` 的 CSS 类名，导致我们在 `app.js` 中无法通过类名直接区分它们。
     2. 重构了实际摘要节点实例的定位算法：不再简单地取 `belongNode._generalizationList[0]`，而是通过遍历 `_generalizationList` 列表，严格对比 `g.generalizationNode.group.node === genEl`（即比对内存中 SVG 封装对象绑定的原生 DOM 元素与当前触发事件的 DOM 元素是否一致）。
     3. 完美实现了 DOM 元素到实际 `actualGenNode` 实例的一一映射。不仅修复了悬浮显示错乱的问题，还连带修复了点击切换不同摘要节点时选中状态（及 `+` 号衍生按钮）错乱的底层隐患。

3. **修复摘要节点鼠标悬浮无法显示备注内容的问题**
   - **问题**: 在摘要节点上添加备注后，鼠标悬浮在备注图标上无法像普通节点一样弹出备注内容悬浮窗。由于底层 `simple-mind-map` 核心库对摘要节点（Generalization Node）的备注事件处理存在遗漏，未能触发自定义的 `customNoteContentShow` 钩子。
   - **解决**: 
     1. 在 `app.js` 的 `renderSummaryPlusButtons` 函数中，对获取到的摘要节点 DOM 元素 (`genEl`) 增加了 `mousemove` 和 `mouseleave` 的事件监听。
     2. 在 `mousemove` 事件中，判断鼠标是否处于备注图标（`.smm-node-note`）内。如果是，则通过 UID 获取真正的摘要节点实例，读取其 `note` 数据，并调用现有的 `showRichNoteTooltip` 渲染出带有富文本与自适应宽度的备注悬浮窗。
     3. 完美复用了已有的备注弹窗逻辑（包含图片自适应、点击放大等），同时不影响普通节点的原有悬浮行为。

2. **节点文字编辑快捷键优化与修复**
   - **功能**: 除了双击节点外，现在选中任意节点（普通节点或浮动节点）后，按下空格键（Space）也能直接进入文字编辑模式。
   - **实现与修复**: 废弃了在 `app.js` 全局 `document` 上监听 `keydown` 事件的旧方案。由于 `simple-mind-map` 核心库自带了一套完善的快捷键注册系统（`mindMap.keyCommand.addShortcut`），且原生支持对输入框焦点的过滤与事件冒泡拦截，因此我们转而通过官方 API 注册 `'Spacebar|Space|space'` 快捷键。
     - 若当前有选中的浮动节点（优先通过 `floatingNodes.find(n => n.data.isActive)` 判断，兼容 `window._selectedFloatingNode`），调用 `startFloatingNodeEdit` 进行内联编辑。
     - 若当前有选中的普通节点，调用 `mindMap.core.textEdit.show()` 或 `mindMap.renderer.textEdit.show()`，完美复用了 F2 的内置编辑逻辑，彻底解决了全局监听被底层库吞噬导致快捷键失效的问题。

## 2026-06-02

1. **支持备注内插入与动态预览图片**
   - **功能**:
     1. 支持在富文本备注中通过插入Base64格式来显示图片。
     2. 备注悬浮窗中的图片宽度会进行动态自适应调整：如果图片自身宽度小于等于备注内最长的文字行宽度，则显示图片原宽度；如果图片宽度大于最长文字宽度，则等比例缩放图片，使其宽度适应最长文本宽度。
     3. 用户可以点击备注内的图片，弹出一个全屏的图片预览模态框。
     4. 在预览弹窗中，支持原图尺寸查看，并可通过鼠标滚轮自由放大或缩小图片，同时支持鼠标拖拽移动图片以查看细节。
     5. 点击图片或遮罩层外部空白处即可快速关闭预览弹窗。
   - **实现**:
     1. 修改 `app.js` 中 `wangEditor` 的配置，为 `MENU_CONF.uploadImage` 增加 `base64LimitSize` 限制（允许最大10MB的图片直接转为Base64），并在工具栏添加了 `insertImage` 和 `uploadImage` 按钮。
     2. 优化 `showRichNoteTooltip` 函数：在显示富文本备注悬浮层时，首先临时隐藏图片以精确测量纯文本的最大宽度，随后恢复图片显示，并根据文本宽度与基础内边距的对比，动态调整悬浮层 `.rich-tooltip` 的宽度，利用 CSS 的 `max-width: 100%` 特性实现图片自适应缩放。
     3. 开发了原生的 `showImagePreview` 函数与关联的 `.image-preview-overlay` CSS样式，使用原生的 `wheel` 事件和 `transform: translate() scale()` 实现了图片的滚轮缩放与鼠标拖拽漫游，利用事件委托实现了点击外部区域关闭功能。

## 2026-05-31

1. **桌面应用化与系统托盘支持**
   - **功能**:
     1. 将项目打包为了单文件可执行程序 (`launcher.exe`)，并生成了自定义 AI 图标。
     2. 安装后会在系统的“开始”菜单中生成快捷方式。
     3. 程序启动后在后台运行，系统托盘右下角会出现程序图标，右键菜单支持“打开 AI Mind”和“退出”功能。
     4. 实现了单实例运行限制：每次双击快捷方式不会重复启动多个 Python 服务进程。
     5. 优化了唤起体验：如果程序已在运行，再次点击快捷方式时，系统不会在浏览器中盲目打开新标签页，而是自动在已打开的所有窗口中寻找“AI Mind - 思维导图”的浏览器页面，并将其置于前台显示（唤醒现有标签）。
   - **实现**:
     1. 编写了 `launcher.py`，使用 `win32event.CreateMutex` 实现了 Windows 平台下的单实例互斥锁。
     2. 通过 `pystray` 和 `PIL.Image` 构建了系统托盘图标及右键菜单。
     3. 在后台线程中使用 `uvicorn.Server` 启动 FastAPI 服务，并关闭 reload 机制以适应生产环境。
     4. 引入了 `win32gui` 和 `win32con`。在尝试打开浏览器前，使用 `EnumWindows` 枚举所有可见窗口。若发现窗口标题以 `AI Mind - 思维导图` 开头，则通过 `SetForegroundWindow` 唤醒并聚焦该窗口；仅在未找到现有窗口时才调用 `webbrowser.open` 打开新标签。
     5. 使用 `PyInstaller` 将入口文件打包为 `.exe`，并通过 `win32com.client.Dispatch` 自动化脚本将快捷方式写入开始菜单。

## 2026-05-30

1. **多行文本Tab键缩进支持**
   - **功能**: 在备注编辑器中选中多行文字后，按下 Tab 键可以实现多行统一增加缩进，按下 Shift + Tab 键可以减少缩进。
   - **实现**: 在编辑器工具栏配置中注册了 `indent` 和 `delIndent` 菜单。拦截了 wangEditor 所在容器的 `keydown` 事件，并在捕获阶段通过代码模拟点击了对应菜单的 DOM 按钮（同时注意维护焦点状态），完美复用了原生的缩进功能。

2. **全局AI配置支持与密码提示修复**
   - **功能**: 
     1. 在页面左下角新增了“设置”按钮。点击后弹出设置面板，允许配置 AI 的 Base URL、API Key 和模型名称。
     2. 修复了浏览器由于输入框启发式分析，错误地将 AI 排版功能和配置项识别为登录表单并提示“保存密码”的问题。
   - **实现**: 
     1. 创建了统一的 `AIService` 类负责管理 API 配置和请求。
     2. 为 `<input>` 元素增加了 `autocomplete="off"` 和 `autocomplete="new-password"`，并为外部包裹了禁止默认提交行为的表单标签（`<form autocomplete="off" onsubmit="return false;">`），有效避免了浏览器的保存密码弹窗。

3. **备注AI排版功能**
   - **功能**: 在备注编辑框的标题栏旁添加了“✨ AI排版”按钮。点击后会在同行展开一个输入框，让用户输入简短的排版规则。
   - **实现**: 如果编辑器内容不为空，提交后系统会自动将用户输入的规则连同编辑器的 HTML 内容一起发送给配置好的 AI 接口，使用定制的 System Prompt 要求 AI 仅做排版而不改变内容，且限制了只能输出基础的 HTML 标签，从而防止 wangEditor 解析失败或展示 Markdown 代码块，成功返回后将自动替换编辑器中的内容。

4. **修复摘要节点面板偶发性无法弹出的问题**
   - **问题**: 用户在点击摘要节点时，偶发性需要点击多次才能弹出右侧属性面板，甚至经常点不出面板。深入分析后发现，原因在于我们在捕获摘要节点点击事件以显示 `+` 号衍生按钮时，粗暴地调用了 `mindMap.renderer.clearActiveNodeList()` 和 `activeNodeCache = []`。这导致了原生点击选中的节点立刻被我们的代码清空，导致属性面板拿不到当前选中的节点数据从而显示“选中节点查看属性”。面板能偶然出现是因为原生 `mousedown` 和 `mouseup` 与自定义 `click` 之间的时序差，或者是因为用户拖拽导致未触发自定义 `click`。
   - **解决**:
     1. 修改了自定义摘要节点点击逻辑，不再简单地清空活动节点列表。
     2. 在捕获到摘要节点的点击后，通过 `mindMap.renderer.findNodeByUid` 定位到真正的摘要节点实例 (`actualGenNode`)。
     3. 通过执行原生核心命令 `mindMap.execCommand('ACTIVE_NODE', [actualGenNode.getData('uid')])`，将摘要节点安全、正确地推入核心库的活动节点栈中，并将其推入 `activeNodeCache`。
     4. 在监听核心库的 `node_active` 事件中，增加了对 `isGeneralization` 类型的过滤。防止核心库触发节点激活事件时误把摘要节点当作普通节点从而错误地清空 `_activeGenUid`（这会导致原本应该显示的 `+` 号意外隐藏）。
   - **效果**: 彻底解决了摘要节点无法选中、属性面板无法显示的问题。现在只需轻轻单击一次摘要节点，属性面板和 `+` 号衍生按钮都会如丝般顺滑地同时出现。

2. **优化备注悬浮层样式与交互**
   - **宽度动态自适应**: 将悬浮层（`.rich-tooltip`）的宽度从固定最大宽度修改为动态适应文本宽度（`width: max-content`），使其随内部最宽文本行自动撑开，并将最大宽度限制放宽至原来的3倍（`max-width: 1260px`），提升宽表格或长单行文本的阅读体验。
   - **修复悬浮层意外消失问题**: 
     1. 将备注悬浮层 (`.rich-tooltip`) 的 CSS 属性 `pointer-events` 改为 `auto`，使其能接收鼠标事件。
     2. 引入了延迟隐藏机制（300ms）。当鼠标离开图标时，不会立即隐藏悬浮层。
     3. 引入了 `isMouseInTooltip` 状态标识。当鼠标进入悬浮层时（`mouseenter`），设置状态并清除隐藏定时器；在隐藏函数 `hideRichNoteTooltip` 中增加拦截，只要鼠标在悬浮层内，就会屏蔽底层画布误触带来的隐藏请求。
     4. 鼠标彻底移出悬浮层（`mouseleave`）后立即隐藏悬浮层。彻底解决了阅读长备注时悬浮层意外消失的问题。

2. **防止备注编辑意外丢失**
   - **问题**: 在备注编辑框中编辑内容后，如果不小心点击了模态框外部的空白处或按下了 `Escape` 键，编辑框会直接关闭，导致未保存的内容丢失。
   - **解决**:
     1. 在打开备注编辑器时，记录初始化的 HTML 内容状态。
     2. 增加了修改状态检测 (`isNoteModified`)，如果内容发生了改变：
        - 拦截点击空白遮罩层（`overlay`）和 `Escape` 快捷键的关闭行为，并弹出 Toast 提示：“备注已修改，请点击保存”。
        - 对于用户主动点击的右上角 `X` 按钮或“取消”按钮，增加 `confirm` 二次确认弹窗：“备注有未保存的修改，确定要丢弃吗？”，防止用户误操作导致数据丢失。
     3. 如果备注内容没有任何修改，点击空白处、取消按钮或按 `Escape` 键依然可以像以前一样直接关闭编辑框，保持流畅体验。

7. **AI排版规则默认持久化**
   - **功能**: 在备注的AI排版功能中，用户最后一次写入的排版规则会被保存，并在后续所有打开的排版输入框中作为默认内容显示。
   - **实现**: 在执行 AI 排版请求前，将当前输入的排版规则写入 `localStorage` (`ai_format_last_rule`)。同时去除了每次排版成功后清空输入框的逻辑。在点击“✨ AI排版”按钮展开输入框时，如果当前输入框为空，则自动从 `localStorage` 中读取并填充最后一次保存的规则。

8. **修复AI排版时编辑器崩溃与添加生成中断提示**
   - **问题**: 
     1. 有时候 AI 排版返回的内容包含了 Markdown 代码块（如 ` ```html `），或者由于编辑器正处于选中状态，导致 `wangEditorInstance.setHtml()` 解析异常，抛出 `Cannot find a descendant at path` 的严重报错。
     2. 用户在 AI 生成期间如果点击了遮罩层、取消、保存或按下 `Escape` 键，没有拦截提示，容易造成状态不一致。
   - **解决**:
     1. **净化 AI 输出**: 接收到 AI 内容后，去除可能包含的 Markdown 标记格式。
     2. **容错注入**: 捕获 `setHtml()` 可能引发的内部异常。如果报错，降级使用 `wangEditorInstance.clear()` 清空编辑器并调用 `dangerouslyInsertHtml(result)` 安全插入，防止崩溃。
     3. **支持请求中断**: 引入 `AbortController` 来控制 AI 请求，并使用全局变量 `isAIGenerating` 标识状态。
     4. **拦截关闭动作**: 封装了 `handleNoteModalClose` 方法。如果正处于生成中，弹出 `confirm` 询问：“AI正在生成排版内容，确定要中断并退出吗？”。用户只有点击确定才会调用 `abort()` 终止请求并退出编辑器，否则留在当前界面继续等待生成。对于点击“保存”按钮的情况也做了相应的阻断和询问逻辑。

## 2026-05-29

1. **修复摘要节点添加子节点问题与无限极衍生需求**
   - **问题**: 在摘要节点上点击 `+` 号时，新节点错误地生成在原所属分支（如子主题1.2）下方。深入逆向分析后发现，`simple-mind-map` 核心库底层硬编码屏蔽了对 `isGeneralization` 节点的 `insertChildNode` 操作，导致无论如何调整插入参数，新节点都无法在逻辑和渲染上挂载为摘要节点的子节点。
   - **解决 (最终方案)**: 我们创新地**扩展了原有的浮动节点（自由节点）系统**：
     1. 引入了“依附型浮动节点”（`_attachedTo` 属性），使浮动节点能够逻辑依附于任意目标（如概要节点或其他浮动节点）。
     2. 拦截了点击概要节点 `+` 号的行为，改为动态创建一个依附在其右侧的浮动节点，并自动绘制连线。
     3. 在 `app.js` 的视图与数据更新钩子（`view_data_change`, `data_change`）中加入了重计算逻辑（`updateAttachedFloatingNodesPositions`），使得衍生节点能在拖拽、折叠、缩放时完美跟随主导图的变动。
   - **无限极衍生与连线优化**: 
     - 为所有处于激活状态的浮动节点（特别是衍生节点）渲染了 `+` 按钮，用户点击即可继续向右侧不断创建新的衍生依附节点，成功实现了无限极延伸。
     - 修复了衍生节点之间缺少连线的问题：优化了浮动关系连线（`renderFloatRelationLines`）的底层垃圾回收逻辑，防止浮动节点之间的关系被错误清除。
     - 优化了衍生节点的坐标计算逻辑和连线外观：使生成的节点水平间距完全参照主导图标准（约40-60px间距），并将这些依附型连线渲染为类似于主分支的**平滑贝塞尔曲线（无箭头）**，完美模拟了原生的子节点生成效果。

2. **支持垂直结构图与摘要节点建立关系线**
   - **布局方向适配**: 动态读取 `mindMap.opt.layout`。当检测到使用 `organizationStructure`（组织结构图）或 `timeline2` 等垂直布局时，衍生的新节点和 `+` 号按钮会自动改为向下垂直生成，连线的贝塞尔曲线也会自动适配为垂直方向计算。
   - **突破摘要节点的关系线限制**: 
     - 核心库自带的 `associativeLine` 不支持以摘要节点作为起点或终点。
     - 我们重构了自定义 SVG 关系线系统，将其扩展为“支持绑定在任何包含 `_relations` 数据结构上的节点”。
     - 实现了对摘要节点的 Ctrl 选中拦截，如果用户在普通节点和摘要节点（或自由节点和摘要节点）之间尝试建立连线，系统会自动降级采用自定义的浮动节点连线引擎绘制关系线，从而实现了所有类型节点之间都能自由建立连线的需求。

3. **关系线样式优化与控制点拖拽支持**
   - **消除“歪歪扭扭”现象**: 修正了自定义连线（普通节点间或跨自由节点间）的数学算法。放弃了原先不自然的法线计算偏移，改用平滑的二次贝塞尔曲线（`Q` 指令）进行渲染。
   - **全面对齐原生关系线交互**: 
     - 用户现在点击任意一条通过自定义 SVG 层创建的关系线时，除了能改变颜色和样式外，系统会在曲线上生成一个**可拖拽的控制点句柄（红圈）**。
     - 拖拽该控制点可以自由改变贝塞尔曲线的控制点（Control Point）坐标，从而调整出任意弧度和形状的曲线，彻底满足了“像普通节点连接普通节点那样可以调整形状”的需求。控制点数据会自动保存在 `_relations` 数据结构中实现持久化。
     - 修复了选中关系线后，点击画布其他区域无法取消选中并恢复原色的问题（通过在 `draw_click` 与 `node_active` 中增加对自定义关系线的重置逻辑）。
     - 调小了关系线箭头的尺寸，使其视觉比例更协调。
     - 在关系线属性面板中，新增了“显示箭头”的勾选配置项，用户可自由选择是否显示箭头。

4. **文档状态持久化**
   - **记录当前打开的文档**: 在 `app.js` 中新增了通过 `localStorage` (`ai_mind_last_opened_uid`) 保存当前活动文档 UID 的逻辑。
   - 每次切换、新建或加载文档时，会自动更新该缓存。刷新页面初始化（`init()`）时，会优先读取该 UID 并尝试打开上次浏览的文档，避免了刷新后总是跳回第一个文档的体验问题；如果文档已被删除，则安全回退到默认行为。

5. **节点图标样式统一**
   - **对齐原生备注图标**: 将浮动节点（自由节点、衍生节点）中原本自定义渲染的简易备注图标，替换为了与原生普通节点完全一致的 SVG 路径（折角文档样式）。并且统一了图标的尺寸 (`14x14`)、颜色（`#94a3b8`）及对齐位置，确保了整个思维导图中不论是原生节点还是浮动节点，其备注提示在视觉体验上完全一致。

6. **修复面板交互与历史记录状态**
   - **面板点击消失修复**: 修正了在节点属性面板中，通过颜色选择器（Color Picker）调整“字体颜色”、“背景颜色”、“边框颜色”以及“分支线颜色”等属性时，由于触发了错误的实时渲染（`mindMap.render()` 与 `setData()` 产生数据循环更新）导致面板突然消失失焦的问题。现在将视图更新和历史记录压栈拆分为了 `input` 和 `change` 两个事件流，保证了滑动调色板的平滑性且不再意外关闭。
   - **历史记录/撤销问题修复**: 优化了 `autoSave()` 与关联线自动保存时对 `setData` 的滥用。原逻辑中由于数据变化时的自动保存，导致撤销栈（Undo History）被过量的不必要状态淹没。通过将隐式的样式数据（如首次点击关联线赋予的默认颜色）直接静默写入数据层 (`Object.assign(node.data, ...)`) 而非调用 API 强制刷新，成功恢复了原生顺畅的多级撤销（Undo/Redo）功能。

7. **备注编辑器交互优化**
   - **禁用换行自动缩进**: 修复了在备注（富文本编辑器）中，由于使用了缩进功能后，按下回车键（Enter）换行时下一段落会自动继承上一段缩进样式的问题。
   - 通过在 `app.js` 中将回车键（Enter）事件的拦截从配置层移动到外层 DOM 捕获阶段，成功覆盖了 `wangEditor` 内部的默认换行行为。配合 `SlateTransforms` API 实现了手动换行，并在换行时强制清除新段落的所有附加属性（如 `indent`、`padding` 等），彻底解决了无论是否按下 Tab 键，只要换行光标就能回到最左侧的排版痛点。


8. **文件树管理与文件夹支持**
   - **新增文件夹功能**: 在工具栏的新建按钮处增加了下拉菜单，提供“新建导图文件”和“新建导图文件夹”两个选项。
   - **文件树状态持久化**: 在后端 storage.py 中引入了 _tree.json 作为文件系统结构的持久化存储，并且与磁盘上的物理 .json 文件进行了智能合并与同步，避免了数据不一致的情况。增加了 /api/mindmaps/tree 接口用于保存最新的文件树结构。
   - **拖拽排序与移动**: 实现了前端侧边栏文件列表的拖放（Drag and Drop）功能，用户可以自由拖拽导图文件或文件夹来调整顺序，或者将文件拖入文件夹中进行分类管理。
   - **文件夹级联删除**: 支持删除文件夹时级联删除内部包含的所有导图文件，确保数据彻底清理。
